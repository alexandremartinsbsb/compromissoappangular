// O conteúdo do arquivo para o ambiente atual irá substituí-los durante a compilação.
// O sistema de compilação é padrão para o ambiente dev que usa `environment.ts`, mas se você fizer
// `ng build --env = prod` e` environment.prod.ts` serão usados em vez disso.
// A lista de quais env mapeia para qual arquivo pode ser encontrado em `angular-cli.json`.

export const environment = {
  production: false
};
